; 打开Git Bah Here，输入git --version命令，可以查看git软件的版本

; 想让git管理此项目，需要把这个文件夹，初始化成本地仓库(**进入要管理的文件夹**） git init

; 进行项目开发

; 完后需要让git管理代码，形成一个版本，在形成版本之前，可以使用git status查看代码文件的状态

; git本地仓库中有三个区域  工作区 暂缓区 版本库

; 把工作的代码扔到暂存区 git add .   然后git status查看状态显示绿色
